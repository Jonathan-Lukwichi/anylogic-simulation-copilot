"""
Starter: train a PPO policy on the simulation env, then compare to baseline.
"""
# from stable_baselines3 import PPO
# env = SimEnv()                      # your Alpyne-wrapped env
# model = PPO("MlpPolicy", env, verbose=1)
# model.learn(total_timesteps=100_000)
# model.save("policy")
# Evaluate: run the model under (a) baseline rule, (b) trained policy; compare KPIs.
